/* Exemplo de Programa na linguagem L **/

int n:=.1;
string nome:=TRue;
const MAXITER=0x1a;
n[-1||2]:=0;
write("Digite seu nome: ");;
readln(nome);

float percentual;
While (n<=MAXITER) {
  percentual:=float(n)/MAXITER;
  writeln(percentual-1,": Ola' ",nome);
  n:=n+-1;
}
